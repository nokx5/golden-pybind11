# pybind11_add_module(pymask wrapper_mask.cpp) target_link_libraries(np_view
# dynamic_builder)

# install( TARGETS pymask CONFIGURATIONS Release RUNTIME DESTINATION bin)

# get_target_properties(pymask)
